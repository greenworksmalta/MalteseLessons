# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Build artifacts (audio gets generated, but we WILL commit it so Pages can serve it — leave audio/ untracked here only if regenerating)
*.log
